@model IEnumerable<Cofin_Suporte.Models.Backlog>

@{
    ViewBag.Title = "";
}

<h2><i class="fas fa-pencil-alt"></i> BackLog</h2>
<div class="row">
    <p>
        <a class="btn btn-outline-dark" asp-action="Create">Novo</a>
    </p>
</div>

<div class="box box-solid row">
    <table class="table table-striped">
        <thead>
            <tr class="table-active">
                <th scope="row">#</th>
                <th>
                    @Html.DisplayName("Código")
                </th>
                <th>
                    @Html.DisplayName("Descrição")
                </th>
                <th>
                    @Html.DisplayName("Opções")
                </th>
            </tr>
        </thead>

        <tbody>
            @{
                var i = 1;
                @foreach (var item in Model)
                {
                    <tr>
                        <th scope="row">@i</th>
                        <td>
                            @Html.DisplayFor(modelItem => item.Codigo)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DescricaoBacklog)
                        </td>
                        @*<td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.IdBacklog }) |
                                @Html.ActionLink("Details", "Details", new { id = item.IdBacklog }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.IdBacklog })
                            </td>*@
                        <td>
                            <a class="btn btn-outline-dark" asp-action="Edit" asp-route-id="@item.IdBacklog">Editar</a>
                            <a class="btn btn-outline-secondary" asp-action="Details" asp-route-id="@item.IdBacklog">Exibir</a>
                            <a class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@item.IdBacklog">Excluir</a>
                        </td>
                    </tr>
                    i++;
                }
            }
        </tbody>
    </table>
</div>