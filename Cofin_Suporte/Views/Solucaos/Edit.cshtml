@model Cofin_Suporte.Models.Solucao

@{
    ViewData["Title"] = "";
}

<h4>Solução</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdSolucao" />
            <div class="form-group">
                <label class="control-label">Descrição Solução</label>
                <input asp-for="DescricaoSolução" class="form-control" />
                <span asp-validation-for="DescricaoSolução" class="text-danger"></span>
            </div>
            <div class="float-right">
                <a class="btn btn-outline-secondary" asp-action="Index">Voltar</a>
                <input type="submit" value="Salvar" class="btn btn-outline-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
