@model Cofin_Suporte.Models.Categoria

@{
    ViewData["Title"] = "";
}

<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayName("Descrição Categoria:")
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DescricaoCategoria)
        </dd>
    </dl>
</div>
<div class="float-right">
    <a class="btn btn-outline-secondary" asp-action="Index">Voltar</a>
    <a class="btn btn-outline-dark" asp-action="Edit" asp-route-id="@Model.IdCategoria">Editar</a>
</div>